---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app ac-infinity-fansync
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      ac-infinity-fansync:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/lukeevanstech/ac-infinity-fansync
              tag: 1.0.0
            env:
              TZ: ${TIMEZONE:=UTC}
              LOG_LEVEL: INFO
              # Sync interval in seconds
              SYNC_INTERVAL: "60"
              # Intake will run at this percentage of exhaust speed
              INTAKE_RATIO: "0.85"
              # Controller names as shown in AC Infinity app
              # TODO: Update these to match your actual controller names
              EXHAUST_CONTROLLER: "Garage Main Rack"
              EXHAUST_PORT: "1"
              INTAKE_CONTROLLER: "Garage Comms Rack"
              INTAKE_PORT: "1"
            envFrom:
              - secretRef:
                  name: ac-infinity-fansync-secret
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
            resources:
              requests:
                cpu: 5m
                memory: 32Mi
              limits:
                memory: 64Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
    service:
      app:
        enabled: false
